# FFM Club - Firebase Authentication Setup Guide

## 📋 Съдържание
1. [Преглед](#преглед)
2. [Структура на файловете](#структура-на-файловете)
3. [Firebase настройки](#firebase-настройки)
4. [Инсталация](#инсталация)
5. [Netlify deploy](#netlify-deploy)
6. [Тестване](#тестване)

## 🎯 Преглед

Този проект използва Firebase Authentication за управление на потребители. Всички файлове са създадени с Firebase SDK v10+ и ES6 модули.

## 📁 Структура на файловете

Трябва да имаш следната структура във твоя проект:

```
ffm-club/
├── index.html
├── login.html (актуализиран)
├── register.html (актуализиран)
├── dashboard.html (нов)
├── style.css
├── js/
│   ├── firebase-config.js (нов)
│   ├── auth.js (нов)
│   └── app.js (нов)
└── README.md (този файл)
```

## 🔥 Firebase настройки

### Стъпка 1: Създай Firebase проект

1. Отиди на [Firebase Console](https://console.firebase.google.com/)
2. Кликни на "Add project" (Добави проект)
3. Дай име на проекта (например: "ffm-club")
4. Следвай стъпките за създаване на проект

### Стъпка 2: Активирай Authentication

1. В Firebase Console, избери твоя проект
2. Отиди на "Authentication" от страничното меню
3. Кликни на "Get started"
4. Избери "Email/Password" като метод за authentication
5. Активирай "Email/Password" и натисни "Save"

### Стъпка 3: Създай Web App

1. В Project Overview, кликни на иконата `</>` (Web)
2. Дай име на app-а (например: "FFM Club Web")
3. Избери "Also set up Firebase Hosting" (по избор)
4. Кликни "Register app"
5. **ВАЖНО:** Копирай Firebase configuration обекта

### Стъпка 4: Конфигурирай Firestore Database

1. Отиди на "Firestore Database" от страничното меню
2. Кликни "Create database"
3. Избери "Start in test mode" (за разработка)
4. Избери локация (препоръчвам `europe-west3` за България)
5. Кликни "Enable"

### Стъпка 5: Конфигурирай Storage (по избор)

1. Отиди на "Storage" от страничното меню
2. Кликни "Get started"
3. Избери "Start in test mode"
4. Кликни "Next" и "Done"

## 🚀 Инсталация

### Стъпка 1: Копирай файловете

Копирай всички създадени файлове в твоя проект:

1. `js/firebase-config.js`
2. `js/auth.js`
3. `js/app.js`
4. `login.html` (замени стария)
5. `register.html` (замени стария)
6. `dashboard.html` (нов файл)

### Стъпка 2: Конфигурирай Firebase

Отвори `js/firebase-config.js` и замени placeholder стойностите с твоите Firebase credentials:

```javascript
const firebaseConfig = {
    apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
    authDomain: "твой-проект-id.firebaseapp.com",
    projectId: "твой-проект-id",
    storageBucket: "твой-проект-id.appspot.com",
    messagingSenderId: "123456789012",
    appId: "1:123456789012:web:abcdef123456"
};
```

**Къде да намериш тези данни:**
- Отиди във Firebase Console
- Избери твоя проект
- Кликни на иконата ⚙️ (Settings) до "Project Overview"
- Избери "Project settings"
- Скролни надолу до "Your apps"
- Виж данните под "SDK setup and configuration"

### Стъпка 3: Провери структурата

Увери се, че файловете са на правилното място:

```
твой-проект/
├── js/
│   ├── firebase-config.js  ← Firebase настройки
│   ├── auth.js             ← Authentication функции
│   └── app.js              ← Main app логика
├── login.html              ← Актуализиран с модули
├── register.html           ← Актуализиран с модули
└── dashboard.html          ← Нов файл
```

## 🌐 Netlify Deploy

### Вариант 1: Deploy чрез GitHub

1. Push файловете в твоя GitHub repository:
```bash
git add .
git commit -m "Add Firebase authentication"
git push origin main
```

2. Отиди в Netlify Dashboard
3. Кликни "Add new site" → "Import an existing project"
4. Избери GitHub и свържи репозиторито
5. Deploy settings:
   - Build command: (остави празно)
   - Publish directory: `/` (root directory)
6. Кликни "Deploy site"

### Вариант 2: Manual Deploy

1. Създай `.netlify.toml` файл в root директорията:

```toml
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

2. Deploy чрез Netlify CLI:
```bash
netlify deploy --prod
```

### Важни настройки за Netlify

След deploy, провери дали работят ES6 модулите:

1. Отиди в Site settings → Build & deploy → Post processing
2. Увери се, че "Asset optimization" НЕ минифицира JS файловете по начин, който би счупил модулите

## 🧪 Тестване

### Локално тестване

За локално тестване, използвай local server (не може да отвориш файловете директно в браузъра заради CORS):

**Вариант 1: Python**
```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000
```

**Вариант 2: Node.js (http-server)**
```bash
npm install -g http-server
http-server -p 8000
```

**Вариант 3: Live Server (VS Code)**
- Инсталирай "Live Server" extension
- Right-click на `index.html` → "Open with Live Server"

След като стартираш local server, отвори `http://localhost:8000` в браузъра.

### Test Checklist

Тествай следните функции:

- [ ] **Регистрация:**
  - Попълни формата с валидни данни
  - Провери дали се появява "Регистрацията е успешна"
  - Провери email-а си за верификационно писмо
  - Провери в Firebase Console → Authentication дали се е създал потребител

- [ ] **Логин:**
  - Влез с току-що създадения акаунт
  - Провери дали се пренасочва към `dashboard.html`
  - Провери дали се показват правилните данни на потребителя

- [ ] **Забравена парола:**
  - Кликни на "Забравена парола?"
  - Въведи email адрес
  - Провери email-а си за reset линк

- [ ] **Logout:**
  - На dashboard страницата, кликни "Изход"
  - Провери дали се пренасочва обратно към начална страница

- [ ] **Защита на страници:**
  - Опитай да влезеш директно в `dashboard.html` без да си logged in
  - Трябва автоматично да те редиректне към `login.html`

## 🐛 Troubleshooting

### Проблем: "Module not found" грешка

**Решение:** Увери се, че:
1. Използваш local server (не file:// protocol)
2. Пътищата към JS файловете са правилни
3. В HTML файловете има `type="module"` в script таговете

### Проблем: Firebase грешка "Configuration object is not valid"

**Решение:** Провери дали:
1. Правилно си копирал Firebase credentials в `firebase-config.js`
2. Няма липсващи или грешни стойности
3. Strings са обградени с кавички

### Проблем: "auth/operation-not-allowed" грешка

**Решение:** 
1. Отиди в Firebase Console → Authentication → Sign-in method
2. Активирай "Email/Password" provider
3. Save промените

### Проблем: "Permission denied" при запис в Firestore

**Решение:**
1. Отиди в Firebase Console → Firestore Database → Rules
2. За разработка, използвай test mode rules:
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 3, 1);
    }
  }
}
```

### Проблем: CORS грешка

**Решение:** 
- НЕ отваряй HTML файловете директно (file:// protocol)
- Използвай local web server (виж секцията "Локално тестване")

## 📞 Поддръжка

Ако имаш въпроси или проблеми:

1. Провери Firebase Console logs
2. Отвори Browser Console (F12) за JavaScript грешки
3. Провери Network tab за failed requests

## 🔒 Сигурност

**ВАЖНО за Production:**

1. **Firestore Rules:** След тестване, актуализирай правилата:
```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

2. **Environment Variables:** На Netlify, използвай Environment Variables за sensitive data

3. **API Key Restrictions:** В Google Cloud Console, ограничи API keys

## ✅ Готово!

Сега твоят FFM Club проект трябва да има пълна работеща authentication система! 🎉

При допълнителни въпроси, не се колебай да попиташ.
